package com.nixondinho.controller;

import javax.validation.Valid;

import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.*;

import com.nixondinho.model.Entidad;
import com.nixondinho.model.Medico;

public interface ControladorInicial {

	/*
	public String listar(Model modelo, Pageable pageable);

	public String eliminarRegistro(String campoClave, Model modelo, Pageable pageable);

	public String cargarRegistro(String campoClave, Model modelo) throws Exception;

	public String actualizaRegistro(Entidad entidadActualizar, BindingResult resultadoValidacion, Model model,
			Pageable pageable) throws Exception;

	public String insertarRegistro(Entidad entidadActualizar, BindingResult resultadoValidacion, Model model,
			Pageable pageable) throws Exception;

	public String cargarFormulario(Entidad entidadActualizar, Model modelo, Pageable pageable) throws Exception;
*/
}
